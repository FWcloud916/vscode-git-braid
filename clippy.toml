# Clippy configuration for Git Braid.
# Run: cargo clippy --workspace --all-targets -- -D warnings
#
# No overrides — the default deny list applies.
# (The former `-A clippy::todo` allowance was removed once the last `todo!()`
# stub was implemented; do not reintroduce it without a tracked reason.)
