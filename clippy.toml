# Clippy configuration for Git Braid.
# Run: cargo clippy --all-targets -- -D warnings

# Allow `todo!()` in stubs — all stubs are tracked by milestone in PROGRESS.md.
# Remove this once M0 and M1 are complete.
# (Until then, this suppresses `clippy::todo` from the default deny list.)
